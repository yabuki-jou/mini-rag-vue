# FR-042 Vue 真实代理闭环验收复盘

> 迁移说明：本文件于 2026-09-17 从
> `https://github.com/yabuki-jou/mini-rag-milvus.git` 的
> `docs/review/FR-042-Vue真实代理闭环验收/验收复盘.md` 复制；源提交为 `2109071`。
> 原始提交历史请在源仓库中查询。

日期：2026-09-17

## 结论

通过。业务请求全部经过 Vite `5173/api` 代理，没有由验收脚本直连 FastAPI 业务端点。

实际完成：注册登录 → 创建项目 → 上传并解析虚构 TXT → 七字段人工确认 → `CONFIRMED` →
创建档案助手会话 → 目录题 → 有据题 → 无据题 → 历史 → 工具日志。

最终结果：

- 三轮状态：`ANSWERED / ANSWERED / REFUSED_NO_EVIDENCE`；
- 有据回答包含“林川”和 1 条同文件脱敏引用；
- 无据回答为固定文案，引用为空；
- 历史为 6 条完整用户/助手消息，历史引用按 MVP 契约恒空；
- 工具日志 2 条，只包含目录与证据两个只读工具；
- 前端深链接返回 `200`；
- 验收后隔离 PostgreSQL Schema、两个临时 Chroma Collection、文件和 Checkpoint 均已清理。

## 轮次记录

### 第一轮

问题：验收脚本读取了响应契约未公开的 `final_chunk_count`，在归档成功后误报失败。

优化：删除内部字段断言，改用 `CONFIRMED` 状态和后续真实检索命中共同验证索引。

### 第二轮

问题：脚本错误要求历史引用与当前响应引用完全相同。

优化：按 DEC-019 和接口设计 §13.4，改为验证历史正文一致且历史引用恒空；当前响应仍严格验证引用。

### 第三轮

问题：复合问题“负责人是谁并负责什么工作”在一次真实模型调用中保守拒答，前两次同题曾通过。

优化：代理闭环烟测收敛为原文单一事实“安全负责人是谁？”。复合问答质量继续由已通过的 17 条固定集承担，不用重复付费烟测替代质量评测。

### 第四轮

结果：完整闭环通过，无新的产品代码问题。

## 证据边界

本次证明真实 Vite 代理、FastAPI、PostgreSQL、Chroma、本地 BGE/Reranker、文件、SQLite Checkpoint
和 DeepSeek 在本机闭环可工作。没有新增浏览器 DOM 点击证据，也不代表长期稳定性或生产部署结论。
