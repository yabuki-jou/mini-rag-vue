# FR-042 Vue 真实代理闭环问题排查与解决方案

> 迁移说明：本文件于 2026-09-17 从
> `https://github.com/yabuki-jou/mini-rag-milvus.git` 的
> `docs/review/FR-042-Vue真实代理闭环验收/问题排查与解决方案.md` 复制；源提交为 `2109071`。
> 原始提交历史请在源仓库中查询。

## 已解决问题

| 问题 | 原因 | 解决方案 |
|---|---|---|
| `uvx` 默认缓存初始化失败 | 默认工具缓存目录权限/状态异常 | 改用独立临时缓存目录 |
| Chroma 依赖在中文路径读取失败 | Windows 路径编码导致资源文件路径失真 | 把一次性运行环境移到 ASCII 临时目录 |
| 读取 `final_chunk_count` 失败 | 该字段不属于公开响应 | 用确认状态和真实检索验证 |
| 历史引用不等于当前引用 | MVP 明确不持久化历史引用 | 历史只校验正文与空引用 |
| 复合问题偶发拒答 | 真实模型对多事实问题存在判定波动 | 闭环烟测改用单一直接证据问题，复杂质量由固定集评测 |

## 固定排查顺序

1. 检查 `8000/5173/8001` 与 `/api/health`；
2. 确认所有业务请求确实使用 `5173/api`；
3. 检查文档是否为 `CONFIRMED`，并确认临时 Collection 有 Chunk；
4. 区分公开 API 契约与内部数据库字段；
5. 按 DEC-019 检查历史引用是否应为空；
6. 对真实模型失败先区分检索为空、判定拒答和工具选择错误；
7. 结束后核验 Schema、Collection、文件和 Checkpoint 全部清理。

## 当前状态

当前无阻塞问题。第四轮代理闭环已通过；提交前审查发现并修复两项契约偏差：后端 Runtime 构造/关闭
阶段的 `AppError` 统一映射为 `ARCHIVE_AGENT_DEPENDENCY_UNAVAILABLE`，执行阶段既有业务错误保持原样；
Vue 工具标签与测试夹具已改为真实工具名及脱敏摘要结构。后端全量 `601 passed, 2 skipped`，Vue 全量
`130 passed`，编译、类型检查、生产构建和 `git diff --check` 均通过。仍保留“无真实 DOM 点击、真实
模型可能波动”两项证据边界。
